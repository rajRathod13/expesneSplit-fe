export const environment = {
  production: false,
  apiBaseUrl: 'https://localhost:7207', // ← your backend base URL
  authPingPath: '/api/secure/ping', // ← ANY [Authorize] endpoint (change to yours)
};
